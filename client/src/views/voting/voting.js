import "./voting.css"

// do this later - doesnt' help MVP purposes -- need to pass in token/userIds and things like that for modulation (cleaner but unecessary for development right now)
export default function voting() {

    return(
        <p>voting page</p>
    )
}